/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // reactStrictMode: true,
    domains: ["source.unsplash.com", "127.0.0.1", "plus.unsplash.com", "images.unsplash.com"],
  },
}

module.exports = nextConfig